// Function whick checks user role and returns allowed documents and denies access to other documents.
const express = require('express');
const app = express();

function checkUserRole(req, res, next) {}

module.exports = checkUserRole;
